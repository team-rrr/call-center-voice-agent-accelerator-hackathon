name: Trigger auto deployment for ca-callcenterpss-4ehqz

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-callcenterpss-4ehqz-AutoDeployTrigger-2f224ca5-c335-4572-af64-b89ef0ba4130.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CACALLCENTERPSS4EHQZ_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CACALLCENTERPSS4EHQZ_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CACALLCENTERPSS4EHQZ_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: callcenterpss4ehqz.azurecr.io
          registryUsername: ${{ secrets.CACALLCENTERPSS4EHQZ_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CACALLCENTERPSS4EHQZ_REGISTRY_PASSWORD }}
          containerAppName: ca-callcenterpss-4ehqz
          resourceGroup: rg-callcenterpss-4ehqz
          imageToBuild: callcenterpss4ehqz.azurecr.io/ca-callcenterpss-4ehqz:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


